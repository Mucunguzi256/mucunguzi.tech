---
import { siteConfig } from "../config";
---

<footer style="padding: var(--space-6); text-align: center; border-top: 1px solid var(--border); font-size: var(--text-sm); color: var(--fg-3); font-family: var(--font-mono);">
  &copy; {new Date().getFullYear()} {siteConfig.name}
</footer>
